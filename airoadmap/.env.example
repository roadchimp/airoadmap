# OpenAI API Key - Required for AI features
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key - Required for Claude AI features (if used)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Database Configuration
DATABASE_URL=your_database_url_here

# Server Configuration
PORT=9000
HOST=127.0.0.1

# Note: When running on Replit:
# 1. Go to "Tools" > "Secrets" in the Replit interface
# 2. Add these environment variables there instead of using a .env file
# 3. The values will be automatically loaded by the application

# For local development:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual API keys
# 3. Make sure not to commit the .env file to version control 